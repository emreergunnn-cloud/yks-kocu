interface Props { university: string; setUniversity: (v: string) => void; department: string; setDepartment: (v: string) => void; ranking: string | number; setRanking: (v: string) => void; }
const inputClass = "w-full rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-2 text-sm text-slate-900 focus:outline-none focus:ring-2 focus:ring-blue-500/50 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-100";
export function TargetSection({ university, setUniversity, department, setDepartment, ranking, setRanking }: Props) {
  return <div className="space-y-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 className="border-b border-slate-100 pb-3 text-base font-bold text-slate-900 dark:border-slate-800 dark:text-white">Üniversite ve Bölüm Hedefleri</h2><div className="grid grid-cols-1 gap-4 sm:grid-cols-2"><label className="text-xs font-medium text-slate-700 dark:text-slate-300">Hedef Üniversite<input type="text" placeholder="Örn. Orta Doğu Teknik Üniversitesi (ODTÜ)" value={university} onChange={(e) => setUniversity(e.target.value)} className={`mt-1 ${inputClass}`} /></label><label className="text-xs font-medium text-slate-700 dark:text-slate-300">Hedef Bölüm<input type="text" placeholder="Örn. Bilgisayar Mühendisliği" value={department} onChange={(e) => setDepartment(e.target.value)} className={`mt-1 ${inputClass}`} /></label><label className="text-xs font-medium text-slate-700 dark:text-slate-300 sm:col-span-2">Hedef Sıralama (Derece)<input type="number" placeholder="Örn. 5000" value={ranking} onChange={(e) => setRanking(e.target.value)} className={`mt-1 ${inputClass}`} /></label></div></div>;
}
